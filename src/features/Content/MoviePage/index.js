// import { BackdropImage, Container, Image, MainInfo, Off, Rate, Rating, Shadows, Star, Title, Votes } from "./Backdrop/styled";
// import posterBig from "../../../images/posterBig.png"
import Backdrop from "./Backdrop";

const MoviePage = () => {
    return (
        <Backdrop title={"Mulan long title"} rate={7.8} votes={335} />
        
    );
};

export default MoviePage;
